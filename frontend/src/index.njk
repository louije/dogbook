---
layout: base.njk
title: Accueil
year: 2024
---

<section class="hero">
  <div class="hero__container">
    <h2 class="hero__title">Trombinoscope Maisons-Doggo</h2>
    <p class="hero__description">Ils sont tous beaux.</p>
  </div>
</section>

<section class="dogs">
  <div class="dogs__container">
    <div class="dogs__grid">
      {% for dog in dogs %}
      <article class="dog-card">
        <a href="/chiens/{{ dog.id }}/" class="dog-card__link">
          <div class="dog-card__image-wrapper">
            <img
              src="{{ dog | getFeaturedPhoto }}"
              alt="{{ dog.name }}"
              class="dog-card__image"
              loading="lazy"
            >
          </div>
          <div class="dog-card__content">
            <h3 class="dog-card__name">{{ dog.name }}</h3>
            {% if dog.breed or dog.age %}
            <div class="dog-card__meta">
              {% if dog.breed %}
              <span class="dog-card__breed">{{ dog.breed }}</span>
              {% endif %}
              {% if dog.breed and dog.age %}
              <span class="dog-card__separator">•</span>
              {% endif %}
              {% if dog.age %}
              <span class="dog-card__age">{{ dog.age }} ans</span>
              {% endif %}
            </div>
            {% endif %}
            {% if dog.owner %}
            <p class="dog-card__owner">Maître : {{ dog.owner.name }}</p>
            {% endif %}
          </div>
        </a>
      </article>
      {% endfor %}
    </div>
  </div>
</section>
