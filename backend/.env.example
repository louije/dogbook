# Session secret - generate a random string
SESSION_SECRET=change-me-to-a-random-string

# Frontend build hook (optional)
FRONTEND_BUILD_HOOK_URL=

# Database with SQLite optimizations:
# - WAL mode for better concurrency
# - NORMAL synchronous for good balance of safety and speed
# - Larger cache for better performance
DATABASE_URL=file:./keystone.db?connection_limit=1&socket_timeout=10&pool_timeout=10

# Web Push Notifications
# Generate VAPID keys with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@yourdomain.com
